export default async function getUserLocation() {
    console.log('linked');
}

// if (navigator.geolocation) {
//     console.log(
//         navigator.geolocation.getCurrentPosition().position.coords.latitude
//     );
// } else {
//     x.innerHTML = 'Geolocation is not supported by this browser.';
